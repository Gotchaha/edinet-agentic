[project]
name = "edinet-agentic"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["edinet-bench"]

[tool.uv.sources]
edinet-bench = { path = "external/EDINET-Bench" }

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
